{
  "name": "cloud-rlm-trueforge",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main-agent.js",
    "run": "npm run build && npm run start",
    "mcp:build": "npm run build",
    "bq-mcp:start": "node dist/mcp-server/gcp-bigquery-mcp.js",
    "bq-mcp:run": "npm run mcp:build && npm run bq-mcp:start"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^9.0.3",
    "@modelcontextprotocol/server": "^2.0.0",
    "@truefoundry/trueforge-sdk": "^0.1.3",
    "google-auth-library": "^10.2.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.2"
  }
}
