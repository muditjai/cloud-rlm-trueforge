{
  "name": "cloud-rlm-trueforge",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main-agent.js",
    "run": "npm run build && npm run start",
    "mcp:build": "npm run build",
    "mcp:start": "node dist/mcp-server/cloud-billing-mcp.js",
    "mcp:run": "npm run mcp:build && npm run mcp:start"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@truefoundry/trueforge-sdk": "^0.1.3",
    "google-auth-library": "^10.2.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.2"
  }
}
